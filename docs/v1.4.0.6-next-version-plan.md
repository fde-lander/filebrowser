# v1.4.0.6-fde Next Version Plan

**Date:** 2026-07-14
**Base:** v1.4.0.5-fde-hotfix (transition reverted to v1.4.0.4-hotfix)
**Author:** 超级猪兔兔
**Source:** MASTER's real-world testing feedback on v1.4.0.5-fde

---

## Overview

v1.4.0.5-fde introduced 8 commits. Testing revealed:
- **Transition fix (Task 1): NEGATIVE** - must rollback
- **Queue compress system (Task 2): PARTIALLY WORKING** - critical bugs
- **Other features: WORKING** - keep

This document records all bugs found + fix plans for v1.4.0.6-fde.

---

## Chapter 1: Transition Fix Rollback (CRITICAL)

### 1.1 Problem

The transition fix (commit 44616283) attempted to fix anonymous/share user
image navigation by modifying 6 points in ExtendedImage.vue. Result:

- **Logged-in users:** Visual quality regression. After transition completes,
  image shows a brief quality shift (like loading from low-res to high-res).
  This causes a subtle but noticeable flicker. V1.4.0.4 does NOT have this.
- **Anonymous/share users:** No confirmed improvement.

### 1.2 Root Cause Analysis (for v1.4.0.6)

The 6 fixes changed the transition engine's timing and error handling:
1. waitForDecode no longer swallows errors → decode timing changed
2. navigateToImage .catch() aborts transition → different flow
3. onImageError guard → changes error display behavior
4. finishTransition verifies image loaded → delays flag setting
5. Old buffer src cleared → may cause re-fetch (quality shift)
6. Default 'fade' + imgB @load → timing changes

The "brief quality shift" is likely caused by Fix 4 (finishTransition
verifies loaded) or Fix 5 (clearing src triggers re-fetch/re-decode).
The old buffer's src was kept for a reason — the browser retains the
decoded bitmap, allowing instant display. Clearing it forces re-fetch.

### 1.3 Decision

**ROLLBACK to v1.4.0.4-hotfix transition behavior.**
All 6 changes reverted. ExtendedImage.vue + Profile.vue restored exactly.

### 1.4 v1.4.0.6 Direction

Do NOT modify transition engine again unless a fundamentally different
approach is found. The current approach of patching error handling makes
things worse for logged-in users. Possible alternatives:
- Investigate if anonymous user issue is actually CORS/auth related (not
  transition logic)
- Consider server-side fix (different image serving for share URLs)
- Accept current v1.4.0.4 transition behavior as-is

---

## Chapter 2: Queue Compress System Bugs (CRITICAL)

### 2.1 Bug A: Folder-level Compress Does Nothing (BLOCKER)

**Symptom:** Right-click folder → Compress Images → dialog shows file list
(expanded correctly) → click Compress button → NOTHING happens.
No API call, no error, no progress. Backend has NO logs.

**Expected:** Should compress all images in the folder.

**Root Cause (preliminary):** The expandItems() method populates
expandedItems[] for display, but the doCompress() method likely still
reads from this.items (or this.selectedFiles mapped to original items)
instead of expandedItems. When folder items are passed to the API,
they're directory paths, not file paths. The backend may silently fail
or the frontend may not send the request at all.

**Fix Direction:** doCompress() must use expandedItems when available.
The API call must send actual file paths, not directory paths.

### 2.2 Bug B: Progress Status Window Lost After Close (CRITICAL — FAILED FIX)

**Symptom (Original):**
Compress starts → progress shows → close dialog → reopen
Compress Images → NO status shown. Backend IS still running.
User has NO way to know backend is compressing.

**Expected:**
Reopening compress dialog should show current queue status
(running, progress X/Y, current file).

---

#### ❌ v1.4.0.5-fde-hotfix Fix Attempt (Commit cbb83649) — FAILED

**What was done:**
- Added `recoverQueueStatus()` method in CompressImages.vue
- Called it from `mounted()` to check backend status on dialog reopen
- If status is "running" or "completed": show progress UI + start polling
- Polling detects "completed" → show success notification
- Polling detects "idle" → stop polling cleanly

**Code added (L309-380):**
```javascript
mounted() {
  if (!this.items || ...) {
    this.recoverQueueStatus();  // Even with no items, check backend
    return;
  }
  this.backupEnabled = state.user?.compressBackup ?? true;
  this.expandItems();
  this.recoverQueueStatus();  // Check backend after expanding
},
async recoverQueueStatus() {
  try {
    const status = await pollStatus();
    if (status.status === "running" || status.status === "completed") {
      this.compressing = true;
      this.progressData = {...};
      this.pollTimer = setInterval(async () => { ... }, 3000);
    }
  } catch (err) {
    // Backend unavailable or no active job
  }
}
```

**Why It Failed (MASTER's Real Test):**
MASTER deployed and tested v1.4.0.5-fde-hotfix. When opening the Compress
Images window after closing it during a running compression, **NO status
bar appeared at all**. The user had no idea if the backend was running
or finished.

**MASTER's Quote (2026-07-14):**
"原因就喺根本就無。我入去压缩图片窗口根本从来无发现有呢个状态栏。
所以呢个FIX係失败了，需要下个版本检讨和重新回滚重新做过呢个功能的方面，
做好调研，然后重新用有效的方法去FIX和做好功能。"

---

#### 🔍 Hypothesized Root Causes (FOR NEXT AI TO INVESTIGATE)

**DO NOT ASSUME ANY OF THESE WITHOUT VERIFICATION.**
The fix was written based on common Vue lifecycle patterns but never
verified to actually work. Possible reasons it failed:

1. **Prompts.vue Component Lifecycle:**
   - CompressImages may be wrapped in `<component :is="...">` with
     `<keep-alive>` (caching) → `mounted()` only fires on first mount
   - If wrapped in `<keep-alive>`, dialog reopen would NOT re-mount
     → mounted() never triggers again
   - Need to check: is CompressImages inside `<keep-alive>` in Prompts.vue?

2. **CompressImages Only Mounted When Prompt Active:**
   - The component may be inside `v-if="prompt === 'compress'"`
   - If so, it should re-mount on each dialog open
   - But if there's a wrapper that caches, that breaks
   - Check: in Prompts.vue, how is CompressImages rendered?

3. **API Permission Issue:**
   - `compressStatusHandler` in backend has admin permission check
     (`!d.user.Permissions.Admin`)
   - If MASTER was testing in some non-admin context, pollStatus returns 403
   - But MASTER was logged in as admin → unlikely cause
   - BUT: response.ok would be false → throws → catch block runs → silent

4. **Silent Error in catch:**
   - `recoverQueueStatus()` catches errors silently
   - If pollStatus fails for ANY reason (network, CORS, auth), the error
     is swallowed with no console.log, no notification
   - MASTER would never know the call failed
   - Add console.error or notify.showError for debugging

5. **Race Condition:**
   - Maybe mounted() runs but at that exact moment backend status is
     "idle" (job completed between close and reopen)
   - Then condition `status === "running"` is false → nothing happens
   - Need to ALSO handle "completed" status → show last result briefly

6. **Compressing=true But Progress View Hidden:**
   - The code sets `this.compressing = true` to show progress UI
   - But what if there's a v-else somewhere that hides it?
   - Need to verify the Progress View actually appears in template

7. **Worker Already Finished Before Reopen:**
   - If job finished while dialog was closed, backend status might be
     "idle" (because Bug C fix preserves "completed" but worker exited)
   - When pollStatus returns "idle", code does NOTHING
   - User sees no feedback at all — matches MASTER's report

---

#### 🔧 ROLLBACK EXECUTED (2026-07-14)

Commit `079cbeda`: Reverted `cbb83649` (the failed fix).
- recoverQueueStatus() method removed
- mounted() restored to v1.4.0.5-fde state (no status check)
- Back to pre-fix behavior (dialog reopen = no status)

**Current Code State (post-revert):**
- mounted() only handles backup preference + expandItems()
- No backend status check at all
- Dialog reopen shows only main dialog (file list, tier selection, etc.)
- No progress UI visible until user clicks Compress

---

#### 📋 v1.4.0.6 REQUIRED ACTIONS for Next AI

**Step 1: PROPER INVESTIGATION (BEFORE writing any fix)**

The previous attempt was based on assumptions. Next AI MUST:

1. **Read Prompts.vue carefully** to understand:
   - How is CompressImages rendered?
   - Is there `<keep-alive>`?
   - What is the component lifecycle when dialog closes + reopens?

2. **Test mounted() actually fires on dialog reopen:**
   - Add console.log("mounted fired") at top of mounted()
   - Build + deploy
   - Open + close + reopen dialog
   - Check browser console — does mounted fire each time?

3. **Test pollStatus API manually:**
   - Run a compression
   - Close dialog (let backend keep running)
   - Use browser DevTools to call GET /api/compress-images/status
   - Verify response shape: does it have `status`, `currentFile`, `processed`, `total`?

4. **Check Vue DevTools:**
   - Is CompressImages component still in memory after close?
   - If yes → keep-alive problem → need activated hook instead of mounted
   - If no → component should re-mount → investigate why polling doesn't trigger

**Step 2: ALTERNATIVE DESIGN APPROACHES**

| Approach | Pros | Cons |
|----------|------|------|
| A: Vue keep-alive + activated hook | Standard Vue pattern | Requires restructuring Prompts.vue |
| B: Global Pinia/Vuex store for queue state | Survives component unmount | Requires new state architecture |
| C: Persistent banner outside the dialog | Always visible when active job | UI redesign |
| D: WebSocket push from backend | Real-time, no polling | Requires backend changes |
| E: Polling from Prompts.vue parent | Component lifecycle-agnostic | Affects multiple prompts |

**RECOMMENDED:** Start with C (persistent banner). It's the least invasive
and most user-visible. If too complex, fall back to A.

**Step 3: VERIFICATION PROTOCOL**

Before declaring any fix done:
1. Start a compression of 50+ files
2. Close dialog after progress shows
3. Wait 5 seconds
4. Reopen dialog
5. Verify: progress UI appears immediately, current file name shown,
   X/Y count matches backend, polling keeps updating
6. Wait for completion → verify notification shown
7. If ANY step fails → STOP and document why

**Step 4: USER TEST GATE**

After deployment, ask MASTER to test:
- Close + reopen during compression
- Close + reopen AFTER completion
- Multiple dialog opens in succession
- No active job → should show normal main dialog

---

#### 🔗 Related Code References

- CompressImages.vue (after revert): mounted() at L309
- compress.js pollStatus(): L93-100 (uses fetchURL with auth)
- compress.go compressStatusHandler(): L561-569
- compress.go queueMgr.getStatus(): L149
- httpRouter.go route: L149
- Prompts.vue (need to investigate how CompressImages is mounted)

### 2.3 Bug C: Progress Stuck at N-1/N (CRITICAL)

**Symptom:** Compress running → shows e.g. "0/3" → progresses to "2/3"
→ stuck there forever. Backend has FINISHED but UI still shows 2/3.
The polling keeps requesting but never gets "completed" status.

**Expected:** When backend finishes, UI should show completion + notification.

**Root Cause (preliminary):** The compressWorker goroutine calls
finishCurrent() which sets status to "completed". But then
setWorkerActive(false) runs immediately after (in the defer),
which sets status to "idle" because queue is empty. This overwrites
"completed" before the frontend's 3-second poll can catch it.

**Fix Direction:** Need to preserve "completed" state long enough for
frontend to catch it. Options:
- Keep "completed" status for 30 seconds before resetting to "idle"
- OR: Add a separate "lastResult" field that doesn't get overwritten
- OR: Frontend treats "idle" after "running" as completion signal

### 2.4 Bug D: Queue Total Count Confusion

**Symptom:** Compress batch 1 running. Open dialog, select more files,
compress → added to queue. But UI shows batch 1's total count until
batch 1 finishes, then shows batch 2 count (e.g. 0/3, stuck at 2/3).

**Expected:** Should show overall queue progress or current batch clearly.

**Fix Direction:** Status API should clearly separate "current batch"
from "queue". Frontend should show both.

### 2.5 Bug E: No Success Notification

**Symptom:** Compression completes but no success notification shown.

**Root Cause:** Related to Bug C — if polling never catches "completed"
status, the notification code never runs.

### 2.6 Bug F: UI Changes Had No Visual Effect (CRITICAL - INVESTIGATED)

**Symptom:** MASTER reports compress dialog UI looks identical to v1.4.0.4.
Preview checkbox still in old position. No queue status bar.

**Investigation Result (2026-07-14 main agent code trace):**

1. **Preview checkbox NOT moved (CONFIRMED):**
   - Plan Task 6 Step 6.3 said to move checkbox from L133-139 to after L75
   - In actual code execution, this step was SKIPPED entirely
   - Checkbox remains at L126-132 (above Options, same as v1.4.0.4)
   - Root cause: AI execution error — step was in plan but not applied

2. **Queue status bar template NOT added:**
   - Plan mentioned "thin bottom bar showing queue status"
   - No template HTML for this was ever written
   - Only the backend API (GET /compress-images/status) exists

3. **CSS duplicate definitions found:**
   - .compress-preview-img defined TWICE: L713 (max-height: 150px, for
     file list thumbnails) and L865 (max-height: 100%, for preview overlay)
   - .compress-preview-size defined TWICE: L721 and L877
   - These duplicates may cause CSS cascade issues depending on specificity

**MASTER's Quote:** "压缩图片窗口的UI确认一下点解修改了完全无效，跟1.4.0.4一模一样"

**Fix Direction:**
1. Actually MOVE the checkbox HTML block (was skipped)
2. ADD queue status bar HTML to template
3. CLEAN UP duplicate CSS definitions
4. VERIFY changes by grepping for new positions after patch

---

## Chapter 3: Working Features (Keep As-Is)

These features from v1.4.0.5-fde are working correctly and should be KEPT:

- ✅ Preview original image: &size=original (no longer blurry)
- ✅ Preview layout: bottom info visible (flex column CSS)
- ✅ Backup toggle persistence: works across sessions, default ON
- ✅ Folder expansion: file list correctly shows expanded images
- ✅ PNG low tier → WebP Q75: no more OxiPNG hang
- ✅ Thumbnail dead code removal: no visual impact, cleaner code
- ✅ Mobile CSS: added but not yet tested
- ✅ SavedBytes tracking: in worker goroutine (may work once Bug C fixed)
- ✅ SSE removal: no more permanent failure from SSE disconnect

---

## Chapter 4: MASTER Development Directives for v1.4.0.6

### 4.1 Queue List API (MASTER Request)

**MASTER Quote:** "后端压缩系统係几好，但系要有一个功能可以取得所有加入到队列的压缩列表咁样找回状态"

**Requirement:** A new API endpoint that returns ALL items in the queue
(not just current job status). This allows the frontend to show:
- Full queue list: each item with its files, status (queued/processing/done)
- Current job progress within the context of the full queue
- When dialog reopens, immediately fetch full queue state

**Proposed API:** GET /compress-images/queue
Returns: { queue: [{ id, files, level, status, progress }], current: {...} }

This is the key to solving Bug B (status lost after close) — the frontend
can ALWAYS fetch the full queue state on dialog mount, regardless of
whether a previous dialog started the job.

### 4.2 Folder Compress Must Work (MASTER Directive)

**MASTER Quote:** "文件夹压缩功能个度肯定係要修复好。多个文件夹也要。"

**Requirement:** Right-click one or multiple folders → Compress Images →
all images within those folders must be compressed. This is the core use
case. Must support:
- Single folder selection
- Multiple folder selection
- Mixed (files + folders)
- Nested subfolders (recursive expansion)

Bug A (folder compress does nothing) is P0 BLOCKER.

### 4.3 UI Investigation Recorded (MASTER Directive)

**MASTER Quote:** "压缩图片窗口的UI确认一下点解修改了完全无效"

**Finding:** Documented in Ch2.6 Bug F above. Three root causes:
1. Checkbox move step skipped during execution (AI error)
2. Queue status bar HTML never written
3. Duplicate CSS definitions may cause cascade conflicts

This is recorded for v1.4.0.6 fix with verification step after patching.

---

## Chapter 5: v1.4.0.6-fde Fix Priority

| Priority | Item | Status (2026-07-14) |
|----------|------|---------------------|
| ✅ FIXED | Bug A: Folder compress does nothing | v1.4.0.5-fde-hotfix Commit 40aab284 |
| ✅ FIXED | Bug C: Progress stuck at N-1/N | v1.4.0.5-fde-hotfix Commit d1b66eea |
| ✅ FIXED | Bug F: Checkbox position | v1.4.0.5-fde-hotfix Commit 56d5b6b2 |
| ❌ REVERTED | Bug B: Status lost after dialog close | cbb83649 failed in production (MASTER saw nothing). Reverted in 079cbeda. See Ch2.2 for full analysis. |
| 🔴 P0 | Ch4.1: Queue List API + dialog reopen UI refinement | MASTER directive — needs clearer UI when reopening with active job |
| P1 HIGH | Bug E: No success notification | Should work now after Bug C fix |
| P1 HIGH | Bug D: Queue count confusion | Depends on Bug B refinement |
| P1 HIGH | CSS duplicate cleanup | Annotated only — per MASTER directive |
| P3 LOW | Transition fix (anonymous users) | Needs new approach |

---

## Chapter 6: v1.4.0.5-fde-hotfix Scope

**Goal:** Revert transition changes only. Keep all other features.

**Actions:**
1. git checkout v1.4.0.4-hotfix -- ExtendedImage.vue Profile.vue
2. Keep all other commits (Task 2-8)
3. Rebuild Docker image as v1.4.0.5-fde-hotfix
4. Save tar + cleanup build cache

**What stays from v1.4.0.5-fde:**
- Queue compress system (with known bugs, to fix in v1.4.0.6)
- Preview original + layout fix
- Backup persistence
- Folder expansion
- PNG low → WebP
- Thumbnail dead code removal
- Mobile CSS
- SavedBytes tracking

**What reverts to v1.4.0.4-hotfix:**
- ExtendedImage.vue (transition engine, all 6 fixes undone)
- Profile.vue (default transition back to 'crossfade')
